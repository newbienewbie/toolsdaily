# 中石化信息标准化网批量查询脚本
this is a query from sinopec info std website
to use the query script,you must first `rename the config.php.bak`
```Bash
mv config.php.bak config.php 
```
and then `modify the username and password` with the 
```bash
vim config.php
```

2014-09-11 : when I push my code to my github repo,I always find that my commit is authorised by sontek.
oh my god ,I git clone his dotfiles repo and have not modified the .gitconfig.
But is that a potential fake bug?  
